# セキュリティについて

## この道具の前提(仕様として認めていること)

**マイコンの制御ポート(TCP 5555)に認証はありません。** 同じ LAN にいる誰でも、
手順の転送・実行・停止・設定変更・ファームウェアの更新(OTA)ができます。これは
見落としではなく、**信頼できる家庭内ネットワークでだけ使う**前提での設計判断です
(認証を入れると鍵の配布と保管が必要になり、この規模の道具では管理の手間の方が
事故のもとになります)。

したがって、次の状態は「脆弱性」ではなく**使い方の誤り**として扱います。

- 制御ポートをインターネットへ露出させた(ポート開放・DMZ・VPN の設定誤り)
- 信頼できない人がいるネットワーク(公衆 Wi-Fi・共用 LAN)で使った

**インターネットに露出させないでください。**

## 報告してほしいこと

上の前提の**内側**で成り立たないことは、直すべき欠陥です。例:

- 同じ LAN にいなくても操作できてしまう経路(例: ブラウザ経由。操作画面は
  他サイトからの操作を Host / Origin と Content-Type で拒みます)
- 手順名やプロジェクト名を細工して、プロジェクトフォルダの外を読み書き・削除
  できてしまう経路
- 細工したパケットで装置が停止・再起動する、あるいは確保していない領域を読み書きする経路
  (実行中に止まると、ボタンが押されたままになる可能性があります)
- OTA の検証を迂回して、任意のファームウェアを恒久的に書き込める経路

## 報告のしかた

**GitHub の Security タブ → Advisories → "Report a vulnerability"**(非公開の
報告)を使ってください。issue を開く画面の案内からも同じ場所へ行けます。
使えない場合は issue で「連絡先だけ」を書いてくだされば、こちらから
連絡します(**詳細を公開の issue に書かないでください**)。

- 少人数で維持している道具です。**返信は数日〜数週間かかることがあります**
- 報奨金の制度はありません
- 直した内容は、コミットの本文に理由と再現条件を書いて公開します

## 対応する版

最新のタグ(現在 `v0.3.0`)だけを直します。過去の版へは戻しません。

## 装置を安全に止める

暴走したときは、**マイコン前面のボタンを 1.5 秒長押し**すると全ボタンを離して
止まります(操作画面の「今すぐ止める」と同じ)。電源を抜いても止まりますが、
その場合は最後に押していたボタンの状態が本体側に残ることがあります。
