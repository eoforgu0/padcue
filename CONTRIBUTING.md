# 手を入れるときに

報告も変更提案も歓迎します。以下は「こう書いてあると助かる」という程度の
もので、厳密な規則ではありません。

## 言語

日本語で構いません。英語でも読みます。コード内のコメントと文書は日本語で
統一しています。

## 変更を送る前に

```powershell
pip install -e "./pctool[dev]"
playwright install chromium

python -m pytest -q                          # 実機なしで通ります
python -m ruff check pctool                  # lint(CI と同じもの)
python pctool/tools/uicheck.py <出力先>      # 画面を実際に動かす検査
```

いずれも実機がなくても通ります。**通った件数を変更の説明に書いてください**
(「pytest 582 / uicheck 134」のように、**自分が流したときの数**を)。件数は
増え続けるので、何件で通ったかが分かると、後から見たときにどの時点の話か
辿れます。

画面を触る変更なら uicheck も、通信やコンパイラを触る変更なら pytest も
必要です。判断に迷ったら両方通してください。

## 実機に触れる検査を書くとき

`pctool/tools/` の検査道具は、**必ず `lock_to_mock()` を通してください**
(`pctool/tools/_localonly.py`)。探索の候補を模擬デバイスに固定し、それでも
取りこぼしたときのために loopback 以外への接続を止める、二重の安全策です。

この安全策が無いと、**検査が実機を選び、接続されている Switch を実際に
操作します**。同じ LAN に実機があると名前解決が本物を指すためで、実際に
起きうる事故です。安全策の外に出る道具を作らないでください。

## コミットの書き方

既存の履歴に合わせてもらえると助かります。

- 1行目は50文字以内。**変わった事実**を書く。`feat:` `fix:` のような
  種別は付けない(変わった事実を読めば種別は分かる)。触った場所が1つの
  層に閉じているときだけ、`ファーム:` `文書:` のように場所を頭に付けてよい
- 本文には「なぜそうしたか」と「採らなかった案があればその理由」を書く
- 本文の行は68桁くらいで折る
- 最後に検証した内容を書く(上記の件数など)
- Co-Authored-By 等の付記は入れない(履歴に1種類の書き方だけを残すため)

## 文書の書き方

- **用語は用語集(`docs/glossary.md`)に従う**。同じものを別の名前で呼ばない。
  新しい語を足すときは用語集にも書く
- 段落は 80 桁で折る(1行が伸びると、差分がその行まるごとになって何を
  直したのか読めなくなる)。表とコードブロックは例外
- コードフェンスには言語を付ける(`powershell` / `text` / `c` など)。
  「貼り付けるコマンド」と「画面に出る文字」が見分けられる
- 図(枠線や矢印)は **幅の決まった文字だけ**で描く。罫線素片(`─│┌`)や
  矢印(`→←`)は環境によって1桁にも2桁にも見えるため、桁がずれる。
  ASCII の `- | + < > ^ v` と全角文字を使う

## リポジトリの構成

| 場所        | 中身                                                |
|-------------|-----------------------------------------------------|
| `pctool/`   | PC 側。`padcue` パッケージ・テスト・開発ツール      |
| `firmware/` | 装置側。ESP-IDF プロジェクト(`pademu`)              |
| `docs/`     | 設計と仕様。一覧は [docs/README.md](docs/README.md) |

## 名前について

3つの名前が出てきます。

- **padcue** — システム全体と、PC 側の道具(コマンド・パッケージ・設定ファイル)
- **pademu** — 装置に書き込むファームウェア(pad emulator)。C の識別子・mDNS 名・
  探索の識別文字列・NVS の名前空間はすべてこちら
- 装置のハードウェアには名前を付けていません(文書もコードも「装置」と呼びます)

「装置に属するものは装置の名前」で分かれています。
