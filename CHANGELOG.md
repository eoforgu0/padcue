# 変更の記録

版の意味は「実機で何ができるようになったか」です。PC 側・ファームウェア・
模擬デバイスの3箇所の版番号は常に同じ値にそろえます(`tests/test_version.py`)。

各版で何を変えたかの詳細はコミットの本文にあります(件名は「変わった事実」、
本文は「なぜ」と「採らなかった案」、末尾に検証した件数)。

## v0.3.0 — 2026-08-14

**公開できる形に整えた版。** 画面の見た目・挙動・機能は v0.2.0 から変えていない。

- **名前を確定** — システム全体と PC 側が `padcue`、装置のファームウェアが
  `pademu`(旧 `padctl` / `switchctl`)。コマンド・mDNS 名・探索の合言葉・
  NVS の名前空間まで一斉に改めた
- **画面の資産を実ファイルへ** — `gui.py` に埋め込んでいた HTML/CSS/JS を
  `pctool/padcue/web/` へ出し、JS は役割ごとに8本へ分けた(6,663 行 → 953 行)
- **操作画面の入口に門番** — 他サイトからの操作を Host / Origin /
  Content-Type で拒む。装置が勝手に動く経路を塞いだ
- **公開物の体裁** — Apache-2.0 + NOTICE(第三者に権利がある要素の切り分け)、
  CONTRIBUTING、CI(GitHub Actions)、issue テンプレート、この記録
- **決まりを検査に** — 版番号の一致・ボタン表の一致・割り込み経路の IRAM 配置・
  文書の書き方(フェンスの言語/表/リンク/図の文字/桁)を機械が見る
- **修正**: 実機(size_t 32bit)でだけ長さ検査を通り抜け、確保外を読む経路
  (`pademu_decode`)/ くり返し回数 0 を両実装で弾く / 装置の収集が想定外の
  例外で黙って止まる / 到達段階の正常値を実測(`0x7fb`)に合わせる

## v0.2.0 — 2026-08-06

**装置2台の同時運用。** 実機2台で検証済み。

- 装置ごとの縦レーン画面(1台のときも同じ構造)
- 「連結」でまとめて開始・自動合流・連動停止、割り当てのプリセット保存
- 装置台帳(個体ID で覚える)。改名しても参照は切れず、別個体への
  黙った乗り換えをしない
- 待機分岐のタイムアウト(無限に待ち続けない)
- 本体識別子の表示と命名(どの Switch につないでいるか)
- 修正: 本体がコントローラー登録を拒む障害(ペアリングの3フェーズ応答)

## v0.1.0 — 2026-08-04

**1台での完成。** ここまでが土台。

- 手順を画面で組み立て、フレーム単位の時刻どおりに実機へ流す
- プロコン方式(ジャイロあり)と HID ゲームパッド方式(保険)の2系統
- 転送・実行・停止・進捗監視・ログ回収、無線更新(OTA)と自動での切り戻し
- 実機なしで全機能を試せる模擬デバイス
- C 実行エンジンと Python 参照実装の送出列一致を検査で保証
