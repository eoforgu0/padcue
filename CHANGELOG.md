# 変更の記録

版の意味は「実機で何ができるようになったか」です。版番号は5箇所に書いてあり
(PC 側・ファームウェア・模擬デバイス・この記録の最新見出し・SECURITY が名指し
する「直す版」)、常に同じ値にそろえます(`tests/test_version.py`)。

各版で何を変えたかの詳細はコミットの本文にあります(件名は「変わった事実」、
本文は「なぜ」と「採らなかった案」、末尾に検証した件数)。

## v0.3.0 — 2026-08-16

**公開できる形に整えた版。** できることは v0.2.0 から増やしていない。画面には
見え方の修正が入っている(下記「画面」)。

- **名前を確定** — システム全体と PC 側が `padcue`、装置のファームウェアが
  `pademu`(旧 `padctl` / `switchctl`)。コマンド・mDNS 名・探索の合言葉・
  NVS の名前空間まで一斉に改めた
- **画面の資産を実ファイルへ** — `gui.py` に埋め込んでいた HTML/CSS/JS を
  `pctool/padcue/web/` へ出し、JS は役割ごとに8本へ分けた(6,663 行 → 953 行)
- **操作画面の入口に門番** — 他サイトからの操作を Host / Origin /
  Content-Type で拒む。装置が勝手に動く経路を塞いだ
- **公開物の体裁** — Apache-2.0 + NOTICE(第三者に権利がある要素の切り分け)、
  CONTRIBUTING、CI(GitHub Actions)、issue テンプレート(脆弱性は非公開の
  報告へ導く)、この記録
- **決まりを検査に** — 版番号の一致(5箇所)・ボタン表の一致・割り込み経路の
  IRAM 配置・文書の書き方(フェンスの言語/表/リンク/図の文字/桁)・空行と桁の
  乱れ(ruff の preview 規則)を機械が見る
- **画面** — 連結中の選択肢を、レーンの選択肢と同じ姿にした(人を待っている
  ことが姿で分かる)。注目の枠を、押す物のある場所へ移した。自ら消える知らせ
  から数字を落とし、閾値(300ms)を超えたズレだけ消えない警告にした。
  連結パネルの保存の光り方を、手順・部品とそろえた(毎秒の描き直しに
  消されない)。人を待っているボタンは枠で名乗るだけにした(塗りまで変えると、
  指を乗せた瞬間に元の色へ戻って壊れて見える)
- **修正**: 実機(size_t 32bit)でだけ長さ検査を通り抜け、確保外を読む経路
  (`pademu_decode`)/ くり返し回数 0 を両実装で弾く / 装置の収集が想定外の
  例外で黙って止まる / 到達段階の正常値を実測(`0x7fb`)に合わせる
- **後片づけ**: 誰も呼んでいない6つ(C の公開 API 1つを含む)を削除。仕様書の
  題名と節を実装済みの実態へ。検査の関数名を全 392 件 ASCII に統一。画面検査
  3,633 行を区画ごとの10ファイルへ分け、**なぜ pytest ではなく独自の仕組みなのか**
  を書き残した(起動コマンドは変わらない)。CSS の散らばった指定を1か所へ
- **公開物としての整理** — 文書・コード・コミット本文から、作業の日付と内部の
  経緯(点検工程の通し番号、実装段階の呼び名、決定の出どころ)を落とした。
  技術的な事実と「なぜそうしたか」は残してある。読む人が辿れないものだけを外した

## v0.2.0 — 2026-08-06

**装置2台の同時運用。** 実機2台で検証済み。

- 装置ごとの縦レーン画面(1台のときも同じ構造)
- 「連結」でまとめて開始・自動合流・連動停止、割り当てのプリセット保存
- 装置台帳(個体ID で覚える)。改名しても参照は切れず、別個体への
  黙った乗り換えをしない
- 待機分岐のタイムアウト(無限に待ち続けない)
- 本体識別子の表示と命名(どの Switch につないでいるか)
- 修正: 本体がコントローラー登録を拒む障害(ペアリングの3フェーズ応答)

## v0.1.0 — 2026-08-04

**1台での完成。** ここまでが土台。

- 手順を画面で組み立て、フレーム単位の時刻どおりに実機へ流す
- プロコン方式(ジャイロあり)と HID ゲームパッド方式(保険)の2系統
- 転送・実行・停止・進捗監視・ログ回収、無線更新(OTA)と自動での切り戻し
- 実機なしで全機能を試せる模擬デバイス
- C 実行エンジンと Python 参照実装の送出列一致を検査で保証
